import { View, Text } from "react-native";

export default function PaymentSuccess() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>✅ Payment Successful (Temporary Screen)</Text>
    </View>
  );
}
