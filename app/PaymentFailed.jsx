import { View, Text } from "react-native";

export default function PaymentFailed() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>❌ Payment Failed (Temporary Screen)</Text>
    </View>
  );
}
